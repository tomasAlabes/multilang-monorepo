// Package graph holds the gqlgen-generated GraphQL server: the executable
// schema (generated.go) and GraphQL models (models_gen.go).
//
// This doc.go is a committed placeholder so the package path resolves on a
// fresh checkout. `gqlgen generate` (run via `nx codegen api-gateway`)
// produces the real files next to it.
package graph
