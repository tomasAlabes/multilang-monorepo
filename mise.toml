# Single source of truth for toolchain versions across every language.
# `mise install` provisions all of these; CI pins the same versions.
[tools]
node = "22"
pnpm = "11.12.0"
go = "1.25"
python = "3.12"
uv = "latest"
buf = "latest"
