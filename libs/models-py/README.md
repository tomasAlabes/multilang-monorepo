# models-py (`greenhouse-models`)

Python message classes and gRPC stubs generated from `libs/proto`.

- **Package:** `greenhouse.v1.*`
- **Consumed by:** `anomaly-detector` (declared via `[tool.uv.sources]`
  workspace dependency and Nx `implicitDependencies`).
- **Graph edge:** `implicitDependencies: ["proto"]`.

Registered as a member of the root `uv` workspace. Generated `*_pb2.py` /
`*_pb2_grpc.py` files are committed — see `libs/models-ts/README.md`.
