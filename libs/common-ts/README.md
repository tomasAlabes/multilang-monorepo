# common-ts (`@greenhouse/common`)

Shared TypeScript utilities (env loading, formatting, `Result` type).
Consumed by `ui` and `dashboard`. Nx infers the graph edge automatically
from the `@greenhouse/common` import — no `implicitDependencies` needed.
