{
  "name": "graphql-schema",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "tags": ["scope:contracts", "lang:graphql"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm dlx graphql-schema-linter libs/graphql-schema/schema.graphql"
      }
    }
  }
}
