# The GraphQL contract is consumed as a resource by the api-gateway (gqlgen)
# and the dashboard (graphql-codegen) shell_command targets.
resources(
    name="schema",
    sources=["schema.graphql"],
)
