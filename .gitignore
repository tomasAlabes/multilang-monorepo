# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
bin/
.next/
out/
*.tsbuildinfo

# Pants
/.pants.d/
/.pids
.pants.workdir.file_lock*

# Go
*.test

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Local / env
.env
.env.local
*.log
.DS_Store

# Nx cache — not used on this branch, but may exist if the `main` branch
# was built in the same working directory.
.nx/

# NOTE: generated protobuf code is committed here for parity with the other
# branches. Idiomatic Pants would .gitignore it — see README.
